# Project state — Wadi Majlas dam break analysis

Last substantive update: 2026-09-23 07:15 (warning-time products added; interim report 62 pages; six sensitivity runs still to finish).
Live iteration: **W2**. Superseded: W1 stays the record of the methodology report Rev 00 and the run sheet; nothing in it is changed.

## Decided (carried from W1)

See `W1/PROJECT_STATE.md` for the methodology decisions (monolith breach, three scenarios × dikes, HEC-RAS 6.6 2D, RCEM, ANCOLD/FEMA).

## Done in W2

- 2026-09-22: "Basin 1 Res2" in HEC-HMS re-pointed to "Terrain Majlas NSA 5m Breach" by editing the basin file with HEC-HMS closed (HEC-HMS rewrites all files on close; see memory note). South catchment added by the user; all "DA <rp> Res2" runs made.
- Hydrographs for the HEC-RAS boundary lines extracted to `W2/04_data/hms_res2_hydrographs.xlsx`: reservoir inflow (FLOW-COMBINE), J11 and Jsouth (FLOW), plus the reservoir outflow as the routing check.
- `W2/01_scripts/data_facts.py` updated with the Res2 peaks and volumes (design-flood table 5-yr to PMF; PMF inflow 18,714 m³/s, 549.4 Mm³; 10,000-yr 14,157 m³/s), the boundary-line peaks (`RES2_PEAKS`), time to peak 14.5 h. Rainfall column unchanged (deck / Flood-Mapping depths). W1's copy is untouched; Rev 00 still quotes the deck's 18,179 m³/s.
- User is preparing the clean HEC-RAS input Excel (PMP and 10,000-yr only: the three hydrographs plus precipitation).

- 2026-09-22, HEC-RAS set-up: user built geometries g02 "Dam Breack" (with dikes, terrain "Terrain (2).Protection2") and g03 "Dan Breack - without dikess" (terrain "Terrain (2)"), 36,769 cells, one 2D area with the dam as an internal SA/2D connection (weir profile 84.5 m over stations 60-260, non-overflow 96.7 m = parapet credited, coefficient 2.2, width 6 m, cells 10-25 m along the line). Breach base case saved in plan p26 (PMPDam): centre 190, width 85, bottom 23, vertical, 1.66, 0.1 h, trigger WS 96.7, linear.
- `W2/01_scripts/ras_setup.py` wrote flow files u26 (fill), u27 (sunny, restart), u28 (PMF, restart), u29 (10,000-yr, restart) and plans p27-p42 (Fill, S1-D/N, S2-D/N, S3-D/N, SW1-3, SW1s, SW3s, ST1-2, SC1, SI1); 60-h windows, 16 cores, SWE-EM, 1-min output, 5-min mapping. `ras_run.py` drives the HECRASController (must call Plan_SetCurrent with the exact title, else HEC-RAS spawns an empty plan). p43 = 1-h test copy of the user's plan, to be deleted.
- Fill run p27: first attempt (6,000 m³/s from a dry start, SWE) went unstable at 4.7 h with the pool at 84.1 m. Second attempt 09:55: ramp 1 h, 2,500 m³/s for 12 h, ramp down, diffusion wave for the warm-up only; restart file expected as `Majlas.p27.01JAN2000 0000.rst` (HEC-RAS naming: project.plan.DDMMMYYYY HHMM.rst) and referenced by u27/u28/u29.
- Lessons logged: the HECRASController needs `Plan_SetCurrent(exact title)` before `Compute_CurrentPlan`, otherwise it creates an empty plan and runs steady; plan/flow/prj files must keep CRLF endings; mesh cells beyond the real count (attribute in 2D Flow Areas/Attributes) are perimeter ghost cells with NaN elevation.
- Scripts in `W2/01_scripts`: `ras_setup.py` (files), `ras_run.py` (driver + run register), `ras_results.py` (HDF → CSV series, 2 m rasters: max depth/velocity/DxV, arrival, duration, AIDR hazard), `ras_analysis.py` (PAR, plots, incremental depth), `ras_maps_def.py` (stored max depth/velocity/WSE maps computed by HEC-RAS on its own terrain, to be registered before each run).

- Plan naming (user request, 2026-09-22 10:25): S1 sunny day failure, S2 flood day PMF failure, S3 PMF no failure; D dikes / N no dikes; suffix = the one change: W<width m>, T<time>, C<coefficient>, F<flood>. Plans: p27 Fill; p28 S1D; p29 S1N; p30 S2D; p31 S2N; p32 S3D; p33 S3N; p34 S2D-W51; p35 S2D-W119; p36 S2D-W153; p37 S1D-W51; p38 S1D-W153; p39 S2D-T18m; p40 S2D-T3m; p41 S1D-C1.44; p42 S2D-F10000.
- Solver deviation from the methodology: all runs use SWE-ELM (equation 1, the user's original setting). SWE-EM stalled at start on a diffusion-wave restart and went unstable at the first trickle over the ogee during a fill; a diffusion-wave fill restart cannot feed the momentum solvers. Fill therefore runs on ELM and stops at about 84.4 m (101.7 Mm³), just below the crest. To be stated in the analysis report.
- Fill p27 (dikes geometry, ELM, 101.7 Mm³, no spill) finished 11:03; restart file `Majlas.p27.31DEC1999 2400.rst`.
- Runs go in parallel on project copies (`ras_make_copies.py`: `HEC-RAS Majlas_run2`, `_run3`, `_test`; text files copied, Terrain and Land Classification linked, project file pruned to the files present). Each copy has its own detached `ras_sequence.py` with `RAS_DIR` set; results still land in `W2/04_data/results/<plan>/` and the shared log `04_data/run_sequence.log`. `wmic` is absent on Windows 11, so HEC-RAS 6.6 ignores the core setting and each solver takes all 32 logical cores.
- The no-dikes geometry (g03, 35,878 cells) cannot start from the dikes restart (g02, 36,769 cells): "ERROR with Restart File". Added on 2026-09-22 11:20: fill plan p43 FillN (g03 + u26) and restart-based flows u30 (sunny, no dikes) and u31 (PMF, no dikes); S1N/S2N/S3N repointed to them (`ras_setup.py --nodikes`). Run3 sequence: p43 → p29 → p31 → p33.
- Stall diagnosis (2026-09-22 11:27–11:37, scratch copies `HEC-RAS Majlas_test`, `_test2`): S3D (p32) froze at start-up (17 s CPU, threads waiting, message file stuck before the adaptive time-step lines). Tests: restart + no rain runs; rain + no restart runs; rain + restart from a fill that carried a zero-rain block runs. So a restart file written by a plan WITHOUT a rain block cannot start a plan WITH one. Fix: the fill flows (u26) and the sunny-day flows (u27, u30) now carry the user's rain block with all values zero (`build_unsteady(..., rain_zero=True)`), so every plan in a restart chain has the block. Zero rain changes nothing hydraulically.
- Flood-day trigger corrected (user question, 2026-09-22 11:50): a fixed "water surface 96.7 m" trigger would never fire if the routed PMF peaks below the parapet. S2 plans now fail at the maximum pool: `ras_sequence.py` reads the matching no-failure run (S3D for S2D and the S2D-* sensitivity runs, S3N for S2N, new p48 S3D-F10000 for S2D-F10000), takes the time of the maximum headwater stage from its `connection.csv`, and writes it as a set-time breach start into the plan just before the run (`ras_setup.set_trigger_from_s3`, record in `results/<plan>/trigger.json`). Consequence: every S3 run precedes its S2 run. Sensitivity plans p34–p36, p39, p40, p42 and p48 use a 36 h window (breach near hour 15–20, 16 h after); base runs keep 60 h.
- 19:19 (user): sensitivity runs now fill the idle folders while S2D runs (dispatcher rule relaxed from strict base-first). Running: S2D (main), S3D-F10000 (copy 2), S2D-W119 (copy 3). Expected: S2D about 02:00 with three solvers sharing, the whole set around 05:00 on 23 Sep.
- **S3D result 18:50** (324 min, volume error 0.002 %): reservoir peaks at **96.06 m a.s.l.** at storm hour 15.25 (0.56 m over the 95.5 crest, 0.64 m under the 96.7 parapet: the dam is not overtopped, the parapet holds); spillway peak **17,323 m³/s** against the 17,000 design figure; the PMF alone floods 27.3 km² of the plain (side wadis and rain included), 44,300 people, H6 14.0 km². S2D trigger set automatically at 15:15 (pool 96.06). S2D started 18:51 in the main folder. Post-processing bug fixed on the way: no-breach plans carry no "Breaching Variables" dataset (`ras_results.py` now tolerates it). Dispatcher bug fixed: it had launched S2D in all three folders (pending list not refreshed per folder); duplicates killed, main run kept.
- Sunny-day family complete 16:18 (all on Courant 0.4–0.9): S1D 45,312 m³/s; S1N 45,346; S1D-W51 31,663; S1D-W153 66,955; S1D-C1.44 42,384 (peaks at the dam, all 0.17 h after the 2.0 h breach start). Flooded area 16.5–17.1 km² in every case: the peak follows the breach width almost in proportion, the flooded plain does not. S3D alone on the machine since 16:18, pinned at the 0.335 s step by the spillway chute (28 m/s in 10 m cells): 20 % at 16:02, about 02:00 at this pace. Proposed and awaiting the user's go: Courant 1–3 for the flood-day family (`ras_setup.py --courant-flood`, restart S3D).
- Dikes decision 14:29 (`04_data/dikes_decision.json`): S1N versus S1D differs by 0.4 % in flooded area (17.11 vs 17.04 km²), 0.4 % in people at risk (23,300 vs 23,200), 0.0 % in median arrival (15 min) and 0.1 % in peak flow (45,346 vs 45,312 m³/s). **S3N and S2N are dropped**; the report carries the comparison as the evidence. Remaining base chain: S3D (running since 13:26) → S2D. S1N: 107 min.
- Campaign rules (user decisions 13:50) and dispatcher (`orchestrate.py`, detached 14:02, log prefix "orch:"): base runs first (S3D, S2D, S3N, S2N), then the full sensitivity set of ten; a folder gets one plan at a time; S2 plans wait for their S3 twin; the S1N-versus-S1D comparison (flooded area, people at risk, median arrival; 5 % threshold; `04_data/dikes_decision.json`) decides whether S3N and S2N are run at all, the reasoning being that dikes built for the 200-year flood cannot contain a dam-break wave and only their steering effect is in question. The dispatcher also re-runs `ras_results` for plans post-processed by the sequences started before the 13:00 fixes and runs the consequence overlay for every plan. Fixed chains were stopped; the copy-3 stopper remains. Expected: base set by about 23:30, sensitivity through the night, all done around 07:00 to 08:00 on 23 Sep.
- Dikes fill (rain-aware) done 13:26, 65 min, restart file 5.2 MB (the rain-less one was 0.9 MB); S3D started on it at 13:26 and computes (the case that stalled at 11:08). Running at 13:30: S3D (main), S1D-W51 (copy 2), S1N (copy 3).
- Draft report rebuilt and rendered 2026-09-23 01:25 with both review batches applied: 42 pages, 16 figures (incl. model extent and HEC-HMS), 13 tables, 6 footnotes; headings flush left, a sentence before every table and list, methodology and model-input chapters in, native list of figures and tables, landscape figures capped at 15 cm so the caption stays on the page. Final build follows the last run.
- Results report builder ready (12:51): `rpt_results.py` + `build_report.py --results` → `05_report/R0/Wadi Majlas Dam Break Analysis Report Rev00.docx`. Every number is read from `results/<plan>/` at build time; a scenario without products is printed as "pending", so the report rebuilds in one command as runs finish. First build with S1D only: 8 figures, 10 tables, 6 footnotes.
- Post-processing chain verified end to end on S1D (12:45): `ras_results` (rasters, series), `ras_analysis` (population at risk 23,200 of whom 22,400 in H4 or worse; 2,951 plots touched, 1,678 residential), `results_charts`, `qgis_result_maps` (6 maps per plan in the client layout). No-dikes fill p43 done 12:39 (61 min, pool 84.51 m, 101.7 Mm³, 3 m³/s trickle); S1N started on it and computes.
- S1D (p28) result 12:18: solver finished, 67 min, volume error 0.007 %, breach peak 45,312 m³/s at 2.17 h (breach at 2.0 h), pool at breach 84.51 m, 102.5 Mm³ through the dam, 20.8 km² wetter than 0.3 m. Products in `04_data/results/p28/`.
- Raster smoothness (user question 12:55): velocity and depth×velocity are now interpolated linearly between wet cell centres (as RAS Mapper does), so hazard classes no longer step at the 10–40 m mesh size; depth was already interpolated; arrival and duration stay cell-based. The 2 m grid carries no more information than the mesh. Information table: anchored to the map's lower-right corner, height computed from the 7 pt font (QGIS `totalHeight()` over-reports before an export), 4 mm right / 6 mm bottom clearance measured on the export.
- Map and report conventions (user review of the first S1D maps, 2026-09-22 12:40): scenario names, never plan codes, in titles, legends, file names and the information table; the two training dikes from the risk maps (`Flood Protection/Risk/Data/SHP/Dykes.shp`, 2 lines), not the older 5-line sketch; hazard classes in the risk-map colours and wording (blue H1 → red H6); the information table stays inside the map frame; **no volume error on maps or in the report** (kept in `summary.json` and the run register only).
- Result statistics (12:35, revised 13:00): `ras_results.py` reports areas, depths, velocities and hazard classes for downstream land only, with reservoir-inclusive figures alongside. The reservoir is defined by geometry (water on the upstream side of the dam-axis centreline at the reference time); an earlier "wet after the breach" rule dropped the trickle-wetted gorge below the dam and made the hazard map start below the dam (user caught it). Rasters cover every wet cell so maps run from pool to sea. Grid: 2 m, same phase as the flood-protection risk rasters (origin 639849.39 / 2601001.69). S1D revised: 17.0 km² downstream (reservoir 3.8 km²), max depth 53.9 m in the gorge, 12.8 m/s, 23,200 people. The stored RAS Mapper maps in each folder (`<short id>/Depth (Max)…`) were computed on the checked terrain "Terrain (2)", not on the dikes terrain, so for dikes plans use the `04_data/results/<plan>/` rasters, which take the terrain from the geometry.
- Unattended chaining (12:20): each folder has a detached `chain.ps1` that waits for a stopper marker, launches the next `ras_sequence.py` list and, for copy 2, copies the rain-aware dikes restart pair before the flood-day list. `ras_sequence.py` now skips a failed plan instead of stopping the folder, and does not wait for an earlier fill when its first plan is a fill. Lists: main p27 → p32 → p30 → p34 → p36 (relaunched 12:21 after my stopper killed the 72 % fill: it matched "START p30" written by copy 2's old sequence in the shared log); copy 2 p37 → p38 → p41, then p48 → p42 → p35; copy 3 (after p43 → p29) p33 → p31 → p40 → p39.
- 11:38 relaunch: main folder runs p27 fill (rain-aware) → p32 S3D → p30 S2D; run3 runs p43 FillN (rain-aware) → p29 S1N → p31 S2N → p33 S3N. run2 finishes p28 S1D on the first (rain-less) fill restart, which is valid because neither side has rain; a detached watcher (`stop_run2_after_p28.ps1`) stops that sequence before it reaches p30. run2 then takes the sunny-day sensitivity runs p37, p38, p41 on the same rain-less pair; the flood-day sensitivity runs (p34–p36, p39, p40, p42) need the new p27 restart copied in first.

## Open

- **Campaign complete 12:04**: S2D-F10000 (106 min alone on the machine, volume error 0.0007 %): breach at storm hour 15:30 on the 93.75 m pool, peak 58,752 m³/s at 15.67 h (S2D at the PMF: 63,858); 26.4 km²; plain depth to 13.1 m; H6 17.4 km²; 43,400 people; the failure wave adds 0.3 m to 24,300 people within 15 min of the breach. Final report built 12:15 from all 14 runs. Combined "arrival fill + H4/H6 outlines" map prepared for the failures (`qgis_result_maps.map_arrival_hazard`, lines from `ras_warning.py --lines`), four variants sent to the user 12:00, decision pending; the PMF version stays a patchwork (rain-fed arrival, no front) and is not recommended.
- **Results 10:16–10:49**: S3D-F10000 (898 min shared): pool 93.75 m at storm hour 15.5, 1.75 m below the crest; spillway 12,386 m³/s; 24.8 km²; 39,100 people; H6 8.7 km²; town reached 3 h 15 min after the storm starts, shoreline 4 h 30 min (reprocessed with the current code; the sequence had the old module). S1D-T3m (127 min): peak 49,525 m³/s at 2.08 h; S1D-T18m (124 min): 42,856 at 2.33 h; both 17.0 km² and 23,200 people like the 6-minute base (45,312), but people reached within 15 min of the breach: 18,050 / 15,579 / 4,871 for 3 / 6 / 18 min, town reached at 10 / 15 / 20 min. S2D-F10000 launched 10:17 in copy 2, trigger 15:30 (pool 93.75 m), the only run left. Report rebuilt 11:00 with the new sensitivity chapter narrative.
- **Sensitivity set re-pointed (user decision 2026-09-23 08:50)**: the sunny-day runs take about an hour, the flood-day ones 12–15 h with three solvers sharing, and the breach mechanics (width, formation time, coefficient) are the dam's question, not the flood's. Stopped S2D-T18m (36 %) and S2D-T3m (24 %), dropped S2D-W51 and S2D-W153, stopped the dispatcher; added **p44 S1D-T3m** and **p45 S1D-T18m** (`ras_setup.py`, built on the existing sunny flow u27, registered in the main and run3 project files) and launched them by hand at 08:40 (main and copy 3, `RAS_DIR` set so they run beside S3D-F10000). Kept: S3D-F10000 (90 % at 08:44) and S2D-F10000, to be launched by hand in copy 2 when S3D-F10000 ends (its sequence sets the trigger from `results/p48/connection.csv`). Report and charts follow the new set: sensitivity table with the four dropped runs marked and the reason, formation-time chart on the sunny day, flood-day width check = S2D-W119 only, executive summary "eight sensitivity runs". Post-processing to run by hand for p44, p45, p48, p42: `ras_analysis.py`, `ras_warning.py`, then charts and the final build. Mishap on the way: the first build call took the "new campaign" branch and minted p49/p50 with duplicate flow files u32–u35, all registered in the project file; removed again within minutes, but that branch overwrote `Majlas.prj.bak_before_dambreak_plans` (the 22 Sep copy of the user's project file before any dam-break plan) with today's file. The current project file still holds every original entry, so nothing is lost from the project; only the historical backup is gone.
- **Repository (2026-09-23 07:45)**: the study folder is a git repository pushed to https://github.com/mojikone/Dam-Break-Majlas (public, user's decision), four commits: root README (project logic, scenarios, models, inputs, outcomes; to be kept current on every meaningful change), inputs and model text files, W1, W2. Excluded by `.gitignore`: HEC-RAS results/restart/terrain/stored maps, HEC-HMS terrain and grids, 2 m result rasters, the W2 docx (104 MB), the design deck, the maritime book, the sample report, archives and backups; 535 MB pushed. Later pushes need the user's explicit go each time (standing rule).
- **Warning time (user, 2026-09-23 06:30; implemented 07:15)**: the hazard maps make the PMF look as bad as a failure, what separates them is time. New `01_scripts/ras_warning.py` writes per plan `results/<plan>/warning.json` (arrival at five named places; people and plots reached within each band, cumulative; people by band × hazard class; 5-min cumulative curve) and `warning_arrival_h.tif`. Definition, the same for every scenario: a place is reached when the depth rises 0.3 m above the pre-event depth (the plan's own state at the breach, or at the storm start for the PMF); the flood-day failure is timed against the no-failure twin at the same instant (the failure wave over the PMF; time bases aligned on the common 36 h). This also removes the fill trickle in the wadi thread, which made the old arrival rasters read 0 at the gorge exit. Results: S1D reaches 15,600 people within 15 min of the breach and 23,200 within an hour (residential centre 15 min, shoreline 22 min); S2D adds 0.3 m over the PMF to 25,400 people within 15 min (shoreline 10 min); S3D needs 3 h for 12,000 people and 12 h for 44,000. Report: new section 9.1 with the places table, the band tables, the band × hazard matrix, the people-versus-time chart (`results_people_vs_time.png`) and the S2D map "arrival of the failure wave over the PMF" (`S2D wave town.png`, `qgis_result_maps.map_warning_arrival`); executive summary, S2D chapter and conclusions carry the numbers. **The running dispatcher does not know `ras_warning.py`: run it by hand for every run that finishes from here on** (`python ras_warning.py p48 p39 ...`; flood-day plans need their twin's HDF, found automatically in the run folders). Caveat printed in the report: the model gives travel time; warning time is shorter by detection and dissemination.
- **Fourth review batch (2026-09-23 05:10–05:40), applied**: new section 4.2 "The dam as designed" (levels, dimensions, 17 monoliths B1 31.21 m / B2–B16 17.00 m / B17 25.79 m with 16 joints, spillway, stilling basin, bottom outlet, reservoir) with a table that says where each value enters the model and three drawings from the design review deck on landscape pages (`02_figures/deck/`: upstream elevation with the monoliths, non-overflow section, spillway section; extracted from `Data/20260921-Majlas Dam Design.pptx` slides 24, 25, 28 with python-pptx and cropped). Chart legends no longer cut at 40 characters (figures 13, 14) and the bar chart uses short scenario labels. Equations keep their lead-in sentence and symbol list on one page; tables of up to 22 rows no longer split; the three methodology flowcharts sit together at the end of chapter 2 so its text pages run without gaps. **Found on the way: the methodology report Rev 00 (W1) shows a gallery detail as its "Figure 2, section through the spillway blocks" and the spillway plan as its "general plan"; W1 is frozen, so this is an erratum for the user to decide on.** Monolith count: the drawing has 17 blocks and 16 joints, so "16" and "17" are both right depending on what is counted; the report says 17 blocks, 16 joints.
- **Interim report 04:40** (`05_report/R0/…Rev00_new.pdf`, 54 pages, 27 figures, 14 tables, 8 equations): S2D chapter, at-a-glance and people-by-hazard charts include S2D; three blank pages removed (adjacent landscape figure blocks are now one section, `build_report.LANDSCAPE`); run register reads the time of long runs from the dispatcher log because HEC-RAS writes "::**" into the HDF summary past 9 h, and marks S3N/S2N "not run: dikes immaterial"; sea-level limitation now quotes MHHW 0.71 m. The `Rev00.pdf` is held open by a viewer, so the export lands in `Rev00_new.pdf` until it is closed (`render_pdf.py` falls back and swaps in when it can; `--render-only` renders pages without Word).
- Timing at 04:35, three solvers sharing 32 threads: S3D-F10000 74 % (about 07:45), S2D-T18m 20 % (about 14:00), S2D-T3m 6 % (about 14:00); then S2D-F10000 in copy 2 (about 17:00) and S2D-W51 / S2D-W153 in the freed folders (about 23:00 on 23 Sep). Every run gets its seven maps on completion; the final build follows the last one.
- **S2D result 04:18** (564 min, volume error 0.00006 %): breach at storm hour 15:15 (pool 96.06 m), peak 63,858 m³/s at 15.33 h against 17,323 without failure; 663 Mm³ through the dam; flooded area 27.1 km² (S3D 25.9) since the PMF already covers the plain; H6 area 18.5 km² against 13.0; people in the flooded area 46,300 against 44,300; plain depth up to 13.5 m (gorge 64.6 m). S2D-W119 (405 min): peak 79,030 m³/s, 27.5 km². "Plain" statistics now east of 693,300 E (the fan start); S2D products were rebuilt with the current code (the main-folder sequence had loaded the older module). Running at 04:20: S2D-T3m (main), S3D-F10000 (copy 2, 74 %), S2D-T18m (copy 3, 18 %); queued S2D-W51, S2D-W153, S2D-F10000.
- Flowcharts (01:50): all four in the report are FigJam boards in one style (title node per column, dense two-line boxes, group-level links, rasterised at 300 dpi and autocropped, placed on landscape pages at 22.9 cm): analysis process, HEC-RAS model build, scenario matrix, run campaign. The Rev 00 boards stay in W1; the W2 boards are listed in `03_maps/figma_boards.md`.
- Third review batch (2026-09-23 01:45), applied: the methodology chapter now carries the Word-native equations (breach weir discharge, Ritter bound, spillway ogee, Courant, D×V with the AIDR class table, population count, dikes-decision test), two Rev 00 flowcharts reused (process, HEC-RAS build) and two new FigJam flowcharts made for this report and rasterised at 300 dpi with the FigJam handles cropped (`02_figures/flowcharts/fc5_scenarios.png`: the three base scenarios with their default conditions in parallel columns; `fc6_campaign.png`: the run campaign with the dikes decision gate; boards in `03_maps/figma_boards.md`; group titles must be nodes inside the groups and links must join groups, not nodes, or FigJam breaks the columns). New charts: inputs (rain and the three inflow hydrographs, PMF and 10,000-yr), base scenarios at a glance (peak, area, people), people by hazard class per scenario.
- Second review batch (2026-09-23 01:00), applied: map extents cover the whole 2D area (reach 1:60,000 moved 2 km south; town 1:32,000); labels with a white halo on boundary lines, dam connection, dikes and 2D area; the information table's first row reads "S1D (sunny day failure, with dikes)"; result tables and maps show "max depth gorge / plain" because the 45–56 m maxima sit in the gorge below the dam (plain: about 13 m in S1D; `PLAIN_X0` = 692,600 E); chart legends carry the breach conditions ("S1D: sunny day, base breach 85 m in 6 min, C = 1.66"); pool chart labels staggered; new figure "HEC-HMS model" from the Basin 1 Res2 sqlite (21 subbasins, 20 reaches) and the basin file's element coordinates, the three hydrograph elements highlighted (junction 'Majlas Dam' = reservoir inflow, J11, Jsouth); the two input workbooks (`Data/XLS/Hydrology Data.xlsx`, `FM rainfall 2-PMP.xlsm`) cited in the model-inputs chapter.
- User review of maps, charts and report (2026-09-23 00:00–01:00), all applied in the scripts: arrival rasters interpolated like velocity and the reservoir masked on them; wet patches under 1 ha (rain puddles) dropped from products and statistics (`MIN_PATCH_HA`); reservoir defined purely by the dam-axis side (the arrival condition left rising pool margins unmasked in PMF runs); arrival ramp red → orange → yellow → green → light blue (shorter arrival = more critical); short map titles with the scenario description in the subtitle; information table bottom-left, translucent, 46 mm value column; no-failure arrival maps say "after the start of the storm"; hydrograph charts drawn with a shape-preserving spline and peak labels left of the peaks; new figure "Model extent" from the as-built 2D area, boundary lines and dam connection (`04_data/gis/model_*.geojson`, EPSG:32640); new "people in the flooded area" map per scenario (GHS-POP in magma reversed, flooded outline); report: headings flush left and bullets restyled at style level (`build_report.patch_styles`), a sentence before every table and list, condensed methodology chapter, model-inputs chapter from the files, chapter numbers re-checked. Sea level: MHHW 0.71 m a.s.l. from `Data/Maritime details.xlsx` (Z0 1.89 m, ONHO Maritime Book 2024), cited in the boundary table.
- Sea level: **0.71 m a.s.l.** (Oman maritime book; user, 2026-09-22 19:40; Excel to follow). The shoreline terrain is higher, so the coast stays a normal-depth outflow and the value is reported as an input, not modelled; a cyclone surge coinciding with the PMF remains outside the study.
- Report (user, 19:40): one report only, built after the last run; methodology as applied included; all given inputs (boundary conditions, 2D area, terrain, breach block, hydrographs) reflected from the files; comprehensive but concise, written plainly, no over-explaining, nothing numerical dropped.
- Number of monoliths: the user said 16 (presentation p. 28, B8 to B13 founded at 15 m); `data_facts.N_BLOCKS` says 17 (B1 to B17 on the DECK general plan). The report text says 16; confirm before Rev 00 goes out.
- Design team items from W1 Appendix D (monolith layout, routed PMF level, EAV for the straight axis, design surfaces, survey files).
- HEC-RAS dam-break geometry being built by the user in `HEC-RAS Majlas/`.

## Next

- Finish S1D-T3m, S1D-T18m, S3D-F10000, then launch S2D-F10000 by hand in copy 2; export each run's maps in QGIS (`maps_for_plan`, `map_population`, `map_warning_arrival` for flood-day failures), run `ras_warning.py <plan>`, regenerate the charts, rebuild the report (`build_report.py --results`, `render_pdf.py --results`), check every page, send the PDF.
- Before Rev 00 goes out: confirm the monolith count (16 or 17); delete the scratch copies `HEC-RAS Majlas_test`, `_test2`; the `_run2`/`_run3` copies can go once the last result is post-processed.
